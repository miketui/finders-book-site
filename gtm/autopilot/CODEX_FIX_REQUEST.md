@codex address the final control-plane merge gate in `gtm/autopilot/CONTROL_PLANE_FIXES.md`. Verify each requirement against current code, implement only still-needed fixes, preserve trusted-main checkout and archive path validation, update tests/contracts if needed, run the required validations, remove both temporary merge-gate markdown files when the fixes are complete, and push the fixes to this PR branch. Do not merge the PR.